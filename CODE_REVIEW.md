# av-sync-fixer 完整代码审查

审查日期：2026-09-07。范围：app、CLI、GUI、样本工具及全部测试；未新增修复模式或重写架构。

环境：Windows / NTFS，项目本地 Python 3.13.5、FFmpeg/ffprobe 9.0.1、PySide6 6.11.2。

## 分级发现

### 严重：Windows 批处理包装器可造成命令注入（已修复）

条件：PATH 将 ffmpeg/ffprobe 解析为 `.cmd` 或 `.bat`，媒体路径含 shell 元字符。使用参数列表并不足以阻止 Windows 隐式启动 shell。本次在隔离临时目录中用 `input&ver&rem.mp4` 和只打印固定文本的包装器复现：额外的无副作用 `ver` 命令被执行。项目当前使用的原生 FFmpeg `.exe` 不触发这一包装器问题。

修复：[app/ffmpeg_utils.py](app/ffmpeg_utils.py) 的工具发现跳过批处理，寻找 PATH 中的原生 `.exe`，然后回退项目内工具；无安全候选时报可读错误。两个执行入口在启动前再次拒绝批处理，不启用 `shell=True`。补充四个执行拒绝场景和三个工具发现回退场景，确认拒绝发生在子进程创建之前。原理依据 [Python subprocess 安全说明](https://docs.python.org/3/library/subprocess.html#security-considerations)。

### 重要：损坏媒体丢失尾部后仍报告成功（已修复）

复现：将带 faststart 的 6 秒 MP4 截断一半。ffprobe 仍报告两轨均为 6 秒；修改前，通用和 Bilibili 预设都发布了视频约 2.967 秒、音频约 2.901 秒的输出。只有检查编码、FPS 和两轨相互长度差，无法识别这次内容损失。

修复：[app/ffmpeg_utils.py](app/ffmpeg_utils.py) 集中加入 `-xerror`，错误上升为失败，已有 [app/fixer.py](app/fixer.py) 的失败清理逻辑随即回收临时文件。覆盖四种模式 × 两种预设的损坏输入测试，并检查 CLI 不打印 100% 或成功对比。正常 VFR、async 音频和时间戳修复继续回归。参数依据 [FFmpeg 文档](https://ffmpeg.org/ffmpeg.html#Advanced-options)。

此修复解决已复现的解码错误被容忍问题，不宣称能证明内容完整或抢救严重损坏的录屏。

### 一般：本轮顺带修复

- Windows 子进程未指定窗口策略：探测和转码统一使用 `CREATE_NO_WINDOW`；POSIX 传入 0，继续使用默认行为。两个执行入口均有参数及真实子进程检查。依据 [Python 文档](https://docs.python.org/3/library/subprocess.html#subprocess.CREATE_NO_WINDOW)。
- 多视频轨在 GUI 中缺少明确的导出范围提示：核心策略现在提示“其余视频轨不会写入输出”，由现有 CLI/GUI 展示。保留只处理第一条非封面视频轨的既定功能，不新增轨道选择。新增双视频、双音轨的真实转码测试。
- 仅 ffprobe 缺失时 GUI 标题仍写 FFmpeg：改为显示实际缺失工具，并补充对应恢复状态测试。

### 一般：未修改的限制

- GUI 暂无取消按钮；运行时阻止关闭窗口以保护 QThread。FFmpeg 没有固定转码总时限，若进程卡住，当前界面不能取消它。界面事件循环仍能响应；这与主线程阻塞或已复现线程泄漏不同。
- 正常退出、转码失败、输出探测失败和 CLI 中断会执行清理；强杀、进程崩溃、断电不保证清理，可能留下 `.avsync-*`、`.generating-*` 或测试临时子目录。强杀 GUI 后也不能保证 FFmpeg 子进程已经退出。
- 原子发布依赖同文件系统硬链接。FAT/exFAT 等不支持的目标会报错；当前未增加文件系统预检或替代发布方式。
- 样本工具逐个发布已生成的文件，不是整组文件的事务。发布途中若磁盘或并发操作失败，可能已留下部分有效样本；不会覆盖已有文件，重跑需移走旧样本或指定新目录。
- ffprobe 使用任务要求的 `-v quiet`，有些探测失败只有通用错误提示。未增加失败后的二次探测。
- 输入诊断和生产输出验证以元数据为主；平均/标称 FPS 接近不能确认 CFR，轨道长度接近不能证明实际音画同步。
- `audio-sync` 不会推断真实录制时钟速度，也不自动混音或强行拉齐尾部。损坏包严格失败，不能保证处理所有“越播越偏”的视频。
- 字幕、附件、章节及额外视频轨不导出；Bilibili 奇数宽高会明确拒绝；HDR、透明通道和高级色彩管理未实现。路径仍受操作系统、文件系统及 FFmpeg 版本限制。

### 建议：后续验证方向，未在本轮扩展

- 补充真实设备录制的长时素材、高分辨率素材，以及人工声音/画面内容核对。
- 在更多 FFmpeg 版本、Windows 路径配置和其他操作系统上执行同一回归集；当前结论主要来自本机 Windows / NTFS。
- 未来单独规划取消、卡住任务处理及崩溃恢复；不要以随意的总时长超时误杀正常长视频转码。
- 本轮未发现值得大规模调整的过度设计。策略数据类、集中命令构建与 GUI 界面层分工合理。少量时长/FPS 文本格式代码因显示精度不同而保留，没有引入通用注册器或额外类层级。

## 15 项检查记录

| 项目 | 结论与证据 |
| --- | --- |
| subprocess 安全 | 保持参数列表、关闭 stdin、不使用 shell；已封堵 Windows 批处理例外。不会把媒体路径拼成命令执行。 |
| 空格、中文、特殊字符路径 | 真实转码覆盖空格、中文、emoji、单引号、`$`、`&`、`;`、方括号和前导 `-`；PowerShell 命令预览另有真实参数回显测试。 |
| Windows 兼容 | 原生 exe、UTF-8 输出、无额外控制台；GBK 输出环境中的 emoji 路径已有回归。仍受长路径、文件系统约束。 |
| 缺少工具 | ffmpeg/ffprobe 分别测试；GUI 显示对应缺失工具并恢复选文件状态，CLI 返回可读错误。 |
| 临时文件 | 生成、编码、输出复查失败及中断均有清理测试；损坏媒体新增测试确认最终文件不发布。异常终止限制见上文。 |
| 覆盖源文件 | 命令构建拒绝输入/输出相同；已有目标及并发同名目标均拒绝；临时文件验证后才创建硬链接，源文件哈希有真实测试。 |
| GUI 多线程 | 核心运行在 QThread，控件由主线程槽更新。新测试直接观察控件更新所在的线程，连续五次任务确认 worker 被销毁、无残留子 worker；现有测试确认处理中事件循环继续响应。 |
| FFmpeg 错误 | stderr 独立线程持续排空并有长度限制；非零退出保留关键内容。已修复解码错误被容忍导致虚假成功；大 stderr、回调中断、读线程启动失败也有回收测试。 |
| 无音轨 | 保持无音轨，safe/cfr/timestamp/Bilibili 可执行；audio-sync 明确要求音轨。未知时长差不当作零。 |
| 多音轨 | 全部映射、逐轨 AAC 编码，safe 逐音轨决定 async；不混音。Bilibili 逐轨验证 48 kHz。播放器/平台的音轨选择不由本工具决定。 |
| 多视频轨 | 只处理第一条非封面视频轨；现在明确提示额外视频不导出。双视频/双音轨测试确认保留正确的视频尺寸、音轨数量、顺序和采样率。 |
| VFR 判断 | 分数转为实际 FPS，按 0.1 FPS 容差比较，使用“疑似”措辞；缺少或无效值为未知。真实样本测试另检查逐帧时间戳。 |
| 测试覆盖 | 本轮新增批处理防护、损坏媒体、特殊字符多轨转码、GUI worker 生命周期及缺少 ffprobe 场景。长时内容与跨平台验证仍不足。 |
| 重复逻辑 | 媒体命令、进度解析、编码输出设置、CFR 复查和样本生成助手已有共享实现。批处理检查复用同一规则；GUI 未复制 FFmpeg 业务逻辑。 |
| 过度设计 | 未发现需要推倒重写的结构问题；此次仅修改现有模块和测试。 |

## 验证结果

完整回归：394 项通过，0 项失败，0 项跳过；耗时 109.19 秒。本轮新增 22 个回归场景。

命令：`python -X utf8 -m pytest -q -o junit_family=xunit1 --junitxml=tmp/test-results.xml`。
详细机器报告：[tmp/test-results.xml](tmp/test-results.xml)。Python 依赖检查通过。

## 日常使用判断

适合在本机已验证环境下处理普通 SDR 录屏、生成 CFR/平台兼容文件，并保留源文件后播放确认。当前可作为日常辅助工具；不应把“转码成功”视为实际音画同步或完整内容已得到证明，也不宜用于无人值守地抢救损坏媒体、自动解决所有录制时钟漂移，或要求保留全部视频轨/字幕/HDR 的流程。

本轮修改：`app/ffmpeg_utils.py`、`app/fixer.py`、`gui/workers.py`、三份回归测试（`test_ffmpeg_utils.py`、`test_fix_integration.py`、`test_gui.py`）、`README.md`；新增本报告。
