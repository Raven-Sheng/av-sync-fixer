[pytest]
testpaths = tests
addopts = --basetemp=tmp/pytest
markers =
    integration: real FFmpeg, subprocess media generation, or Qt integration
    private: optional real recordings from samples/private (also integration)
